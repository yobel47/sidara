services:
  app:
    build: .
    volumes:
      - .:/var/www
  web:
    image: nginx:alpine
    container_name: sidara-web
    ports:
      - "8001:80"
    volumes:
      - .:/var/www
      - ./nginx-internal.conf:/etc/nginx/conf.d/default.conf:ro
    depends_on:
      - app
