services:
  app:
    build: .
    volumes:
      - .:/var/www
    ports:
      - "8001:8000"
  web:
    image: nginx:alpine
    container_name: sidara-web
    volumes:
      - .:/var/www
      - ./nginx-internal.conf:/etc/nginx/conf.d/default.conf:ro
    depends_on:
      - app
